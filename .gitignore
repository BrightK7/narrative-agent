venv/
__pycache__/
*.pyc
.env

# 本地运行状态，不纳入版本控制
data/narrative/seen_articles.json
# sentinel_input.json 由 run_report.sh 生成后 commit 到 repo，Sentinel 直接读取

# 保留 positions 示例，忽略真实持仓
data/positions.json

.idea
data/narrative/cron.log